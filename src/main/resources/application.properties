spring.application.name=IronVault

# Keep the JPA session closed after the service layer
spring.jpa.open-in-view=false

# For production: disable the /v3/api-docs endpoint
# springdoc.api-docs.enabled=false
